on: 
  push:
    branches:
      - hw2

jobs:
  my_testing :
    runs-on: ubuntu-latest

    steps:
    - name: Git clone my repo
      uses: actions/checkout@v1

    - name: Prepare project
      run : |
        cd HW2/
        make prepare

    - name: TESTS + COVERAGE
      run : |
        ls -la
        make test